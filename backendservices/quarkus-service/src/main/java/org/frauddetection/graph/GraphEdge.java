package org.frauddetection.graph;

public class GraphEdge {
    // the ledger based record that will ulitmately crash into db and will be saved but will feed the hypothesis graph
    // the 9 original data format, in plain terms one node multiple edges these multiple edges will not be kept alive in ram
    // but somehow the effect is to be propagted to the hypothesis graph
}
